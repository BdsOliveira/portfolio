/**
 * Professional experience. Ships empty, so the Experience section is removed from the
 * document entirely. Adding the first entry makes the section appear with no code change.
 */
export default [];
